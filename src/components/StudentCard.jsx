function StudentCard({name, course, goal}){
    return(
        <main>
          <h2>Name: {name}</h2>
          <h2>Course: {course}</h2>
          <h2>GOAL: {goal}</h2>
        </main>
    );
}
export default StudentCard;